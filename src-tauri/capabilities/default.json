{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability allowing file deletion",
  "windows": [
    "main"
  ],
  "permissions": [
    "fs:default",
    "fs:allow-remove", 
    {
      "identifier": "fs:scope",
      "allow": ["**/*"]
    }
  ]
}